# 《记忆的缝隙》v0.1.0 内部候选版说明

> 渠道：内部候选构建
>
> 正式发布状态：未满足外部门槛，不得公开标记为完成版

## 内容

- 20–30 分钟简体中文 2D 叙事解谜垂直切片；
- 标题与内容提示、四章、尾声、科普页和制作致谢；
- D0–D4 固定退化曲线、标准模式与低扰动模式；
- 键盘完整操作、静音视觉替代、强制颜色冗余、减少动态、大字和牵手替代操作；
- Tiled 地图、五章环境、角色动作、五幅记忆插图与原创 Web Audio 声场；
- 自动存档、损坏存档恢复说明、本地数据二次确认清除；
- Chrome、Edge、Firefox、WebKit 自动化与三视口候选截图。

## 操作

- 移动：WASD 或方向键；
- 交互/继续：E、Enter 或 Space；
- 观察：Shift；
- 背包/日记/地图：I / J / M；
- 暂停：Esc；关闭面板：Q 或面板按钮。

## 构建与部署

- `npm run release:package:internal` 生成可部署的内部候选目录与 SHA-256 清单；
- `npm run release:package -- <PASS 汇总报告>` 仅用于正式公开包，并会检查项目许可证、资产权利状态与外部证据；
- 发布包使用相对资源路径，可部署到任意静态目录；
- 测试构建保留 source map，发布打包目录主动排除 `.map` 文件；
- `release-manifest.json` 记录版本、Git 提交、渠道、文件大小和每个文件的 SHA-256。

## 正式发布前仍需完成

1. 15 名首次玩家测试达到 GDD 完成率、提示率、D3 和主题理解门槛；
2. 纯键盘、静音、颜色冗余、低扰动、减少动态、200% 缩放和屏幕阅读器真人路径登记；
3. 医学专业审核和家属/照护者敏感性阅读，高优先级问题清零；
4. macOS Safari 实机验证；
5. 项目负责人确定真人语音范围、项目许可证并批准所有运行时资产权利状态；
6. 正式发布当日重新验证权威链接并生成最终外部证据汇总。

执行与判定方式见 `docs/release-evidence/README.md` 和 `docs/RELEASE_READINESS.md`。
